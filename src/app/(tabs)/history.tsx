import { ComingSoon } from '@/components/common/ComingSoon';

/** History tab. Search, filter and edit/delete are built in Milestone 9. */
export default function HistoryScreen() {
  return (
    <ComingSoon
      title="History"
      icon="time-outline"
      description="A searchable, filterable list of every expense arrives in Milestone 9."
    />
  );
}
