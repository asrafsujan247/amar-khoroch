import { ComingSoon } from '@/components/common/ComingSoon';

/** Settings tab. Currency, data export/import and reset are built in Milestone 10. */
export default function SettingsScreen() {
  return (
    <ComingSoon
      title="Settings"
      icon="settings-outline"
      description="Currency, data backup and app preferences arrive in Milestone 10."
    />
  );
}
